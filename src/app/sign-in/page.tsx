import Authenticator from "@/features/authenticator";

const SignInPage = () => {
    return (
        <section className="h-full">
            <Authenticator />
        </section>
    )
}

export default SignInPage;
