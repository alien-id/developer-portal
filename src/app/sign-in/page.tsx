import { Authenticator } from "@/features/auth";

const SignInPage = () => {
    return (
        <section className="h-full">
            <Authenticator />
        </section>
    )
}

export default SignInPage;
